{
  "overrides": [
    {
      "files": ["*.ts"],
      "extends": "standard-with-typescript",
      "parserOptions": {
        "project": "./tsconfig.json",  // caminho correto
        "tsconfigRootDir": "."         // raiz do projeto
      }
    }
  ]
}
